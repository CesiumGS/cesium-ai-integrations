/**
 * Geolocation server constants and default values
 */

/** Default search radius in meters */
export const DEFAULT_SEARCH_RADIUS = 5000;

/** Default timeout for browser geolocation requests (ms) */
export const GEOLOCATION_TIMEOUT_MS = 15000;
